---
title: Poll
description: Create and manage native Discord polls with interactive forms. Use Discord's built-in polling system for seamless voting.
keywords: poll, voting, survey, native polls, discord polls, interactive
---

import { Callout } from "fumadocs-ui/components/callout";
import { Tabs, Tab } from "fumadocs-ui/components/tabs";
import { Cards, Card } from "fumadocs-ui/components/card";

## Subcommands

<Cards>
  <Card
    title="create"
    description="Create a new poll using an interactive form"
  />
  <Card title="list" description="List all polls in the server" />
  <Card title="end" description="End an active poll early" />
  <Card title="delete" description="Delete a poll permanently" />
</Cards>

## Usage Examples

<Tabs items={["Create Poll", "List Polls", "Manage Polls"]}>
  <Tab value="Create Poll">
    Create a new poll:
    ```bash
    /poll create
    ```
    This opens an interactive modal form where you can:
    - Select poll duration (1 hour to 7 days)
    - Choose single or multiple choice voting
    - Enter poll question and options (2 to 10 options)
    - Add custom emojis to options
  </Tab>
  <Tab value="List Polls">
    List all active polls in the server:
    ```bash
    /poll list
    /poll list page:2
    /poll list show-ended:true
    ```
    Options:
    - `page`: Page number to display (default: 1)
    - `show-ended`: Include ended polls in the list (`true`/`false`)
  </Tab>
  <Tab value="Manage Polls">
    Manage existing polls:
    ```bash
    /poll end poll-id:1234567890
    /poll delete poll-id:1234567890
    ```
    - **End**: End an active poll early (creator or staff only)
    - **Delete**: Delete a poll permanently (creator or staff only)
  </Tab>
</Tabs>

## Poll Features

### Native Discord Integration

- Uses Discord's built-in poll system
- Identical UI to manual polls created by Discord
- Real-time vote counting and animations
- Automatic expiration handling

### Poll Configuration

**Duration Options:**

- 1 hour - Quick polls
- 2-12 hours - Extended engagement
- 1-3 days - Community decisions
- 5-7 days - Long-term voting (Discord maximum)

**Vote Types:**

- **Single Choice**: Users select one option
- **Multiple Choice**: Users can select multiple options

## Permissions Required

<Callout type="info">
  - Anyone can create or list polls (`/poll create`, `/poll list`).
  - Poll creators and administrators with **Manage Messages** permission can end or delete polls early.
</Callout>
